# armada-forge runtime dependencies.
#
# CPU-ONLY. Nothing here requires a GPU to install or import — "no GPU-required code path
# in any service's default configuration" is a platform-wide non-goal, not a limitation.
# The torch CPU wheel is selected explicitly in the Dockerfile via the CPU index URL;
# installing the default wheel would pull ~2 GB of CUDA libraries that never execute.
#
# Phase 7 adds: transformers, peft, trl, datasets (LoRA SFT, plus in-process candidate
# generation and perplexity per roadmap F1/F2), and llama.cpp's conversion tooling baked
# into the image for GGUF export (F6).

fastapi==0.115.6
uvicorn[standard]==0.34.0
psycopg[binary]==3.2.3
psycopg-pool==3.2.4
pyyaml==6.0.2

# Ingestion
sentence-transformers==3.3.1
pypdf==5.1.0
